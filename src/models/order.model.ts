export interface Order {
    id: number;
    createdAt: string;
    address: string;
    reciver: string;
    contact: string;
    bookTitle: string;
    totalQuantity: number;
    totalPrice: number;
}